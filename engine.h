#ifndef ENGINE_H
#define ENGINE_H
#include "graphics.h"
#include "random.h"

// Preferrably called every frame.
// Will also call update_graphics() from graphics.h.
void update(int ms);

typedef struct {
    
} Object;

#endif